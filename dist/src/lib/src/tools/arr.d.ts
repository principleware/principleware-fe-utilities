/**
 * Pushs an array or a single value into the thisArg;
 */
export declare function pushArray<T>(thisArg: Array<T>, src: Array<T> | T): void;
/**
 * Turns the values in an object into an array
 */
export declare function makeArray(o: Object): any[];
